services:
  - type: web
    name: bot-panaderia
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000